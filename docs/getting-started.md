# 快速开始

本指南帮助你在 5 分钟内启动 Nekro VStack 项目。

---

## 前置要求

- Python 3.11+
- Node.js 18+
- pnpm 8+ (或 npm/yarn)

---

## 自动初始化（推荐）

```bash
./scripts/init-project.sh
```

脚本会交互式地引导你完成：

- ✅ 依赖检查和安装
- ✅ 项目信息配置
- ✅ 环境变量设置
- ✅ 数据库初始化
- ✅ 类型定义生成

---

## 手动初始化

### 1. 安装工具

```bash
# 安装 uv (Python 包管理器)
curl -LsSf https://astral.sh/uv/install.sh | sh

# 安装 pnpm (可选)
npm install -g pnpm
```

### 2. 安装依赖

```bash
uv sync          # Python 依赖
pnpm install     # Node.js 依赖
```

### 3. 配置环境

💡 **无需配置**：项目已包含 `.env` 文件，使用默认配置即可启动。

如需自定义：

```bash
# 编辑配置文件
nano .env

# 或从示例重新创建
cp .env.example .env
```

### 4. 初始化数据库

```bash
mkdir -p data
pnpm db:init
pnpm db:init-db
```

### 5. 生成类型定义

```bash
pnpm generate:types
```

---

## 启动项目

### 一键启动（推荐）

```bash
pnpm dev:all
```

同时启动：

- ✅ 后端服务器（自动生成 OpenAPI）
- ✅ 前端开发服务器
- ✅ 类型文件监听（自动更新）

日志带颜色区分，后端代码变化时类型会自动同步。

### 分别启动

```bash
# 终端 1：后端
pnpm dev:backend

# 终端 2：前端
pnpm dev:frontend
```

---

## 访问应用

- **前端**: http://localhost:5173
- **API 文档**: http://localhost:9871/docs
- **默认账号**: `admin` / `admin`

---

## 常见问题

### 端口被占用

```bash
# 修改端口
pnpm dev:backend -- --port 8000
pnpm dev:frontend -- --port 3000
```

### 数据库初始化失败

```bash
# 重置数据库
rm -rf migrations/ data/db.sqlite3
pnpm db:init && pnpm db:init-db
```

### 类型生成失败

```bash
# 确保后端已启动
pnpm dev:backend

# 另一个终端
pnpm generate:types:server
```

---

## 下一步

- 📖 [开发指南](./development.md) - 学习如何添加功能
- ⚙️ [配置指南](./configuration.md) - 自定义项目配置
- 📝 [命令参考](./commands.md) - 所有可用命令
